meta {
  name: 🧾 🙋 Order - Buscar por ID
  type: http
  seq: 2
}

get {
  url: {{base_url}}/order-service/orders/{{order_id}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{jwt_token}}
}

docs {
  Busca um pedido pelo seu ID (UUID).
  Retorna 404 se o pedido não for encontrado.
  Requer autenticação.
}
