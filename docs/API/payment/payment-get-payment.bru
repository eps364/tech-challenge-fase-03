meta {
  name: 💳 🙋 Payment Service - Get Payment
  type: http
  seq: 2
}

get {
  url: {{base_url}}/payment-service/payments/{{payment_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

docs {
  Consulta status de pagamento pelo ID. Requer autenticação JWT.
}